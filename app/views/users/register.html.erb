<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <%= csrf_meta_tags %>
</head>
<body>
<h1 class="my-4 text-center">Rejestracja</h1>

<div class="Register-Form">
  <div class="card" style="max-width: 500px; margin: 0 auto;">
    <div class="card-body">
      <%= form_with model: @user, url: '/register', method: :post do |f| %>
        <div class="form-group">
          <%= f.label :name, "Imię", class: "form-label" %>
          <%= f.text_field :name, class: "form-control", required: true %>
        </div>
        <div class="form-group">
          <%= f.label :last_name, "Nazwisko", class: "form-label" %>
          <%= f.text_field :last_name, class: "form-control", required: true %>
        </div>
        <div class="form-group">
          <%= f.label :email, "E-mail", class: "form-label" %>
          <%= f.email_field :email, class: "form-control", required: true %>
        </div>
        <div class="form-group">
          <%= f.label :birth_date, "Data urodzenia", class: "form-label" %>
          <%= f.date_field :birth_date, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :password, "Hasło", class: "form-label" %>
          <%= f.password_field :password, class: "form-control", required: true %>
        </div>
        <div class="form-group text-center">
          <%= f.submit "Zarejestruj", class: "btn btn-primary btn-lg" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

</body>
</html>
