<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="Dashboard">
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/dashboard">Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Przepisy", recipes_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Kategorie", categories_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Składniki", ingredients_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to "Wyloguj się", logout_path, method: :delete, class: "nav-link" %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <section class="content-section">
    <h1>Składniki</h1>

    <div class="row">
      <% @ingredients.each do |ingredient| %>
        <div class="col-md-4 mb-4">
          <div class="card">
            <h5 class="card-title"><%= ingredient.name %></h5>
            <p>Nazwa: <%= ingredient.name %></p>
            <p>Kalorie: <%= ingredient.calories %></p>
            <p>Jednostka: <%= ingredient.unit %></p>
            <%= link_to "Pokaż Składniki", ingredient, class: "btn"%>
          </div>
        </div>
      <% end %>
    </div>
    <%= link_to "Dodaj Składnik", new_ingredient_path, class: "btn" %>
  </section>


  <footer>
    <p>Moja Strona &copy; 2024. Wszelkie prawa zastrzeżone.</p>
  </footer>
</div>
</body>
</html>
